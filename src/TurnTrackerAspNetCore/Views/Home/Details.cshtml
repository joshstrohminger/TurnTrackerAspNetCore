@using System.Threading.Tasks
@using TurnTrackerAspNetCore.Middleware
@inject Microsoft.AspNetCore.Http.IHttpContextAccessor HttpContextAccessor
@model TrackedTask
@{
    ViewBag.Title = "Details";
    string cookie;
    if (!HttpContextAccessor.HttpContext.Request.Cookies.TryGetValue(TimezoneOffsetReader.CookieKey, out cookie))
    {
        cookie = "0";
    }
    int minutes;
    int.TryParse(cookie, out minutes);
    var timezoneOffset = TimeSpan.FromMinutes(-minutes);
    ViewBag.TimezoneOffset = timezoneOffset;
}
<h1>@Model.Name</h1>
<div>@(Model.TeamBased ? "Team" : "Individual") Based</div>
<div>
    @if (Model.Period == 0m)
    {
        <span>Non-Periodic</span>
    }
    else
    {
        <span>@Model.Period @Model.Unit</span>
    }
</div>
<div>
    <span>Modified </span>
    <span>@Model.Modified.ToOffset(timezoneOffset)</span>
    <span> - </span>
    <span>Created </span>
    <span>@Model.Created.ToOffset(timezoneOffset)</span>
</div>
<h4>Turns</h4>
@if (Model.Turns.Count == 0)
{
    <p>No turns</p>
}
else
{
    <table class="table table-condensed table-hover table-striped">
        <tr>
            <th>Id</th>
            <th>User</th>
            <th>Taken</th>
            <th>Modified</th>
            <th>Created</th>
            @if (User.Identity.IsAuthenticated)
            {
                <th>Delete</th>
            }
        </tr>
        @foreach (var turn in Model.Turns ?? new List<Turn>())
        {
            @Html.Partial("_Turn", turn)
        }
    </table>
}
@if (User.Identity.IsAuthenticated)
{
    <div class="clearfix">
        <form class="pull-left" asp-antiforgery="true" asp-action="TakeTurn" method="post">
            <input asp-for="Id" type="hidden" />
            <input class="btn btn-primary" type="submit" value="Take Turn" />
        </form>
        <a class="btn btn-success pull-left" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
        <form class="pull-left" asp-antiforgery="true" asp-action="DeleteTask" method="post">
            <input asp-for="Id" type="hidden" />
            <input class="btn btn-danger" type="submit" value="Delete" />
        </form>
    </div>
}
<a class="btn btn-default" asp-action="Index">Tasks</a>

@section footer {
    <span class="timezone">UTC Offset: @timezoneOffset</span>
}